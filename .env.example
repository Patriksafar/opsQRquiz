PORT=3000
ADMIN_SECRET=change-me-to-something-hard-to-guess
PUBLIC_URL=http://localhost:3000

# Neon Postgres connection string, used only by the solo quiz (/solo).
# Create a free project at https://neon.tech and copy the pooled connection
# string. The live multiplayer game does not need this.
DATABASE_URL=postgresql://user:password@host.neon.tech/dbname?sslmode=require
